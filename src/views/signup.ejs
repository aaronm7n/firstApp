<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="author" content="Aaron McClellan">
        <meta name="viewport" content="width=device-width,initial-scale=1.0">
        <link rel="stylesheet" type="text/css" href="/css/signup.css"> <!-- This allows the use of CSS -->
        <title>Signup</title>
    </head>
    <body>
        <% if (type == "") { %>
            <h2 style="color: red"><%= message %></h2>
        <% } else if (type == "error") { %>
            <h2 style="color: red"><%= message %></h2>
        <% } else { %>
            <h2>New User, <%= user %> has been created!</h2>
            <a href="/login">Login</a>
        <% } %>
        <form id="form" action="/signup" method="POST">
            <h1>Signup</h1>
            <input name="email" type="email" autocomplete="off" required placeholder="Email">
            <input name="username" type="text" autocomplete="off" required placeholder="Username">
            <input name="password" type="password" autocomplete="off" required placeholder="Password">
            <input name="confirm" type="password" autocomplete="off" required placeholder="Confirm Password">
            <button type="submit">Signup</button>
        </form>
    </body>
</html>